# Database
DB_HOST=postgres
DB_PORT=5432
DB_USER=postgres
DB_PASS=postgres
DB_NAME=socialdb

# JWT settings
JWT_SECRET=change_this_secret
JWT_EXPIRES_IN=3600s

# App
NODE_ENV=development
PORT=3000

# Frontend (build-time arg for Vite)
VITE_API_URL=http://localhost:3000

# Notes:
# - Copy this file to .env and replace secrets before running in production.
# - For production, prefer encrypted secrets, Docker secrets, or your cloud provider's secret manager.
